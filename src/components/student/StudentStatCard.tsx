export default function StudentStatCard({
  label,
  value,
  hint,
  color = "blue",
  "use client";

  import React from "react";

  export default function StudentStatCard() {
    return null;
  }
blue: "bg-blue-50",
